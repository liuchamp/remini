// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Order = {
  id: number | undefined;
  buyerId: number | undefined;
  sellerId: number | undefined;
  productId: number | undefined;
  amount: number | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type CreateOrderRequest = {
  buyerId: number | undefined;
  sellerId: number | undefined;
  productId: number | undefined;
  amount: number | undefined;
};

export type CreateOrderResponse = {
  order: Order | undefined;
};

export type GetOrderRequest = {
  id: number | undefined;
};

export type GetOrderResponse = {
  order: Order | undefined;
};

export type ListOrdersRequest = {
  page: number | undefined;
  pageSize: number | undefined;
  userId: number | undefined;
};

export type ListOrdersResponse = {
  orders: Order[] | undefined;
  total: number | undefined;
};

export type ListMyOrdersRequest = {
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListMyOrdersResponse = {
  orders: Order[] | undefined;
  total: number | undefined;
};

export type PayOrderRequest = {
  id: number | undefined;
  escrowRef: string | undefined;
};

export type PayOrderResponse = {
  order: Order | undefined;
};

export type ShipOrderRequest = {
  id: number | undefined;
};

export type ShipOrderResponse = {
  order: Order | undefined;
};

export type CompleteOrderRequest = {
  id: number | undefined;
};

export type CompleteOrderResponse = {
  order: Order | undefined;
};

export type CancelOrderRequest = {
  id: number | undefined;
};

export type CancelOrderResponse = {
  order: Order | undefined;
};

export type RaiseDisputeRequest = {
  id: number | undefined;
  raisedBy: number | undefined;
  reason: string | undefined;
};

export type RaiseDisputeResponse = {
  disputeId: number | undefined;
  orderId: number | undefined;
  status: string | undefined;
};

export interface TradingService {
  CreateOrder(request: CreateOrderRequest): Promise<CreateOrderResponse>;
  GetOrder(request: GetOrderRequest): Promise<GetOrderResponse>;
  ListOrders(request: ListOrdersRequest): Promise<ListOrdersResponse>;
  ListMyOrders(request: ListMyOrdersRequest): Promise<ListMyOrdersResponse>;
  PayOrder(request: PayOrderRequest): Promise<PayOrderResponse>;
  ShipOrder(request: ShipOrderRequest): Promise<ShipOrderResponse>;
  CompleteOrder(request: CompleteOrderRequest): Promise<CompleteOrderResponse>;
  CancelOrder(request: CancelOrderRequest): Promise<CancelOrderResponse>;
  RaiseDispute(request: RaiseDisputeRequest): Promise<RaiseDisputeResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createTradingServiceClient(
  handler: RequestHandler
): TradingService {
  return {
    CreateOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/orders`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "CreateOrder",
      }) as Promise<CreateOrderResponse>;
    },
    GetOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TradingService",
        method: "GetOrder",
      }) as Promise<GetOrderResponse>;
    },
    ListOrders(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/orders`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TradingService",
        method: "ListOrders",
      }) as Promise<ListOrdersResponse>;
    },
    ListMyOrders(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/orders/mine`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "TradingService",
        method: "ListMyOrders",
      }) as Promise<ListMyOrdersResponse>;
    },
    PayOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}/pay`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "PayOrder",
      }) as Promise<PayOrderResponse>;
    },
    ShipOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}/ship`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "ShipOrder",
      }) as Promise<ShipOrderResponse>;
    },
    CompleteOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}/complete`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "CompleteOrder",
      }) as Promise<CompleteOrderResponse>;
    },
    CancelOrder(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}/cancel`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "CancelOrder",
      }) as Promise<CancelOrderResponse>;
    },
    RaiseDispute(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/orders/${request.id}/dispute`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "TradingService",
        method: "RaiseDispute",
      }) as Promise<RaiseDisputeResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
