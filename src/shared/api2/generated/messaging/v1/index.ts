// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Message = {
  id: number | undefined;
  conversationId: number | undefined;
  senderId: number | undefined;
  content: string | undefined;
  createdAt: string | undefined;
};

export type Conversation = {
  id: number | undefined;
  participantA: number | undefined;
  participantB: number | undefined;
  lastMessage: Message | undefined;
  createdAt: string | undefined;
};

export type SendMessageRequest = {
  conversationId: number | undefined;
  senderId: number | undefined;
  content: string | undefined;
};

export type SendMessageResponse = {
  message: Message | undefined;
};

export type GetConversationRequest = {
  id: number | undefined;
};

export type GetConversationResponse = {
  conversation: Conversation | undefined;
};

export type ListConversationsRequest = {
  userId: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListConversationsResponse = {
  conversations: Conversation[] | undefined;
  total: number | undefined;
};

export type ListMessagesRequest = {
  conversationId: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListMessagesResponse = {
  messages: Message[] | undefined;
  total: number | undefined;
};

export interface MessagingService {
  SendMessage(request: SendMessageRequest): Promise<SendMessageResponse>;
  GetConversation(request: GetConversationRequest): Promise<GetConversationResponse>;
  ListConversations(request: ListConversationsRequest): Promise<ListConversationsResponse>;
  ListMessages(request: ListMessagesRequest): Promise<ListMessagesResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createMessagingServiceClient(
  handler: RequestHandler
): MessagingService {
  return {
    SendMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/messages`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MessagingService",
        method: "SendMessage",
      }) as Promise<SendMessageResponse>;
    },
    GetConversation(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/conversations/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MessagingService",
        method: "GetConversation",
      }) as Promise<GetConversationResponse>;
    },
    ListConversations(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/conversations`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MessagingService",
        method: "ListConversations",
      }) as Promise<ListConversationsResponse>;
    },
    ListMessages(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.conversationId) {
        throw new Error("missing required field request.conversation_id");
      }
      const path = `api/v1/conversations/${request.conversationId}/messages`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MessagingService",
        method: "ListMessages",
      }) as Promise<ListMessagesResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
