// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ProductRecommendation = {
  id: number | undefined;
  productId: number | undefined;
  score: number | undefined;
  reason: string | undefined;
};

export type ViewRecord = {
  id: number | undefined;
  userId: number | undefined;
  productId: number | undefined;
  createdAt: string | undefined;
};

export type GetHomeRecommendationsRequest = {
  userId: number | undefined;
  limit: number | undefined;
};

export type GetHomeRecommendationsResponse = {
  recommendations: ProductRecommendation[] | undefined;
};

export type GetProductRecommendationsRequest = {
  productId: number | undefined;
  limit: number | undefined;
};

export type GetProductRecommendationsResponse = {
  recommendations: ProductRecommendation[] | undefined;
};

export type TrackBehaviorRequest = {
  userId: number | undefined;
  behaviorType: string | undefined;
  targetId: number | undefined;
  targetType: string | undefined;
};

export type TrackBehaviorResponse = {
  success: boolean | undefined;
};

export type GetUserProfileRequest = {
  userId: number | undefined;
};

export type CategoryAffinity = {
  category: string | undefined;
  score: number | undefined;
};

export type PriceRangeMessage = {
  minPrice: number | undefined;
  maxPrice: number | undefined;
  avgPrice: number | undefined;
};

export type TagPreference = {
  tag: string | undefined;
  weight: number | undefined;
};

export type GetUserProfileResponse = {
  userId: number | undefined;
  categoryAffinities: CategoryAffinity[] | undefined;
  priceRange: PriceRangeMessage | undefined;
  tags: TagPreference[] | undefined;
  activityScore: number | undefined;
};

export type GetRecommendationExplanationRequest = {
  productId: number | undefined;
  userId: number | undefined;
};

export type ContributingFactor = {
  factor: string | undefined;
  weight: number | undefined;
  label: string | undefined;
};

export type GetRecommendationExplanationResponse = {
  primaryReason: string | undefined;
  strategy: string | undefined;
  contributingFactors: ContributingFactor[] | undefined;
};

export type RefreshRecommendationsRequest = {
  userId: number | undefined;
};

export type RefreshRecommendationsResponse = {
  success: boolean | undefined;
};

export type GetTrendingProductsRequest = {
  category: string | undefined;
  limit: number | undefined;
};

export type GetTrendingProductsResponse = {
  recommendations: ProductRecommendation[] | undefined;
};

export type BatchRecommendationsRequest = {
  productIds: number[] | undefined;
  userId: number | undefined;
  limit: number | undefined;
};

export type BatchRecommendationsResponse = {
  recommendations: ProductRecommendation[] | undefined;
};

export interface RecommendationService {
  GetHomeRecommendations(request: GetHomeRecommendationsRequest): Promise<GetHomeRecommendationsResponse>;
  GetProductRecommendations(request: GetProductRecommendationsRequest): Promise<GetProductRecommendationsResponse>;
  TrackBehavior(request: TrackBehaviorRequest): Promise<TrackBehaviorResponse>;
  GetUserProfile(request: GetUserProfileRequest): Promise<GetUserProfileResponse>;
  GetRecommendationExplanation(request: GetRecommendationExplanationRequest): Promise<GetRecommendationExplanationResponse>;
  RefreshRecommendations(request: RefreshRecommendationsRequest): Promise<RefreshRecommendationsResponse>;
  GetTrendingProducts(request: GetTrendingProductsRequest): Promise<GetTrendingProductsResponse>;
  BatchRecommendations(request: BatchRecommendationsRequest): Promise<BatchRecommendationsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createRecommendationServiceClient(
  handler: RequestHandler
): RecommendationService {
  return {
    GetHomeRecommendations(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/recommendations/home`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "GetHomeRecommendations",
      }) as Promise<GetHomeRecommendationsResponse>;
    },
    GetProductRecommendations(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.productId) {
        throw new Error("missing required field request.product_id");
      }
      const path = `api/v1/recommendations/products/${request.productId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "GetProductRecommendations",
      }) as Promise<GetProductRecommendationsResponse>;
    },
    TrackBehavior(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/recommendations/track`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "RecommendationService",
        method: "TrackBehavior",
      }) as Promise<TrackBehaviorResponse>;
    },
    GetUserProfile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.userId) {
        throw new Error("missing required field request.user_id");
      }
      const path = `api/v1/recommendations/users/${request.userId}/profile`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "GetUserProfile",
      }) as Promise<GetUserProfileResponse>;
    },
    GetRecommendationExplanation(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.productId) {
        throw new Error("missing required field request.product_id");
      }
      const path = `api/v1/recommendations/${request.productId}/explain`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "GetRecommendationExplanation",
      }) as Promise<GetRecommendationExplanationResponse>;
    },
    RefreshRecommendations(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.userId) {
        throw new Error("missing required field request.user_id");
      }
      const path = `api/v1/recommendations/users/${request.userId}/refresh`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "RecommendationService",
        method: "RefreshRecommendations",
      }) as Promise<RefreshRecommendationsResponse>;
    },
    GetTrendingProducts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/recommendations/trending`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.category) {
        queryParams.push(`category=${encodeURIComponent(request.category.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "GetTrendingProducts",
      }) as Promise<GetTrendingProductsResponse>;
    },
    BatchRecommendations(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/recommendations/batch`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.productIds) {
        request.productIds.forEach((x) => {
          queryParams.push(`productIds=${encodeURIComponent(x.toString())}`)
        })
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RecommendationService",
        method: "BatchRecommendations",
      }) as Promise<BatchRecommendationsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
