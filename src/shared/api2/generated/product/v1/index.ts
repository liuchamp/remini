// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Product = {
  id: number | undefined;
  name: string | undefined;
  description: string | undefined;
  price: number | undefined;
  stock: number | undefined;
  category: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type CreateProductRequest = {
  name: string | undefined;
  description: string | undefined;
  price: number | undefined;
  stock: number | undefined;
  category: string | undefined;
};

export type CreateProductResponse = {
  product: Product | undefined;
};

export type GetProductRequest = {
  id: number | undefined;
};

export type GetProductResponse = {
  product: Product | undefined;
};

export type ListProductsRequest = {
  page: number | undefined;
  pageSize: number | undefined;
  category: string | undefined;
};

export type ListProductsResponse = {
  products: Product[] | undefined;
  total: number | undefined;
};

export type UpdateStockRequest = {
  id: number | undefined;
  delta: number | undefined;
};

export type UpdateStockResponse = {
  product: Product | undefined;
};

export type UpdateStatusRequest = {
  id: number | undefined;
  status: string | undefined;
};

export type UpdateStatusResponse = {
  product: Product | undefined;
};

export type ListMyProductsRequest = {
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListMyProductsResponse = {
  products: Product[] | undefined;
  total: number | undefined;
};

export type SearchProductsRequest = {
  query: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type SearchProductsResponse = {
  products: Product[] | undefined;
  total: number | undefined;
};

export type DeleteProductRequest = {
  id: number | undefined;
};

export type DeleteProductResponse = {
};

export interface ProductService {
  CreateProduct(request: CreateProductRequest): Promise<CreateProductResponse>;
  GetProduct(request: GetProductRequest): Promise<GetProductResponse>;
  ListProducts(request: ListProductsRequest): Promise<ListProductsResponse>;
  UpdateStock(request: UpdateStockRequest): Promise<UpdateStockResponse>;
  UpdateStatus(request: UpdateStatusRequest): Promise<UpdateStatusResponse>;
  ListMyProducts(request: ListMyProductsRequest): Promise<ListMyProductsResponse>;
  SearchProducts(request: SearchProductsRequest): Promise<SearchProductsResponse>;
  DeleteProduct(request: DeleteProductRequest): Promise<DeleteProductResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createProductServiceClient(
  handler: RequestHandler
): ProductService {
  return {
    CreateProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/products`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ProductService",
        method: "CreateProduct",
      }) as Promise<CreateProductResponse>;
    },
    GetProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/products/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ProductService",
        method: "GetProduct",
      }) as Promise<GetProductResponse>;
    },
    ListProducts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/products`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.category) {
        queryParams.push(`category=${encodeURIComponent(request.category.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ProductService",
        method: "ListProducts",
      }) as Promise<ListProductsResponse>;
    },
    UpdateStock(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/products/${request.id}/stock`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "ProductService",
        method: "UpdateStock",
      }) as Promise<UpdateStockResponse>;
    },
    UpdateStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/products/${request.id}/status`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "ProductService",
        method: "UpdateStatus",
      }) as Promise<UpdateStatusResponse>;
    },
    ListMyProducts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/products/mine`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ProductService",
        method: "ListMyProducts",
      }) as Promise<ListMyProductsResponse>;
    },
    SearchProducts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `api/v1/products/search`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.query) {
        queryParams.push(`query=${encodeURIComponent(request.query.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ProductService",
        method: "SearchProducts",
      }) as Promise<SearchProductsResponse>;
    },
    DeleteProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `api/v1/products/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "ProductService",
        method: "DeleteProduct",
      }) as Promise<DeleteProductResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
